const fs=require('fs'),cp=require('child_process'),{ESLint}=require('eslint');(async()=>{const lint=new ESLint();for(const p of ['src/app/dashboard/delivery-releases/page.tsx','src/app/dashboard/product-units/page.tsx']){for(const mode of ['baseline','current']){const input=mode==='baseline'?cp.execFileSync('git',['show',`HEAD:${p}`],{encoding:'utf8'}):fs.readFileSync(p,'utf8');const [r]=await lint.lintText(input,{filePath:p});console.log(`${p} ${mode}: ${r.errorCount} errors, ${r.warningCount} warnings`);}}})().catch(e=>{console.error(e);process.exitCode=1});
